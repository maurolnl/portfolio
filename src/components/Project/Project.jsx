import styles from './Project.module.css'
import global_styles from '../../styles/Home.module.css'

const Projects = () => {
  return(
    <h1 className={`${styles.container_projects} ${global_styles.container}`}>
      Hello
    </h1>
  )
}

export default Projects